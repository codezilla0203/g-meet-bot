# Server Configuration
PORT=5000
RUNTIME_ROOT=./runtime

# Base URL for email verification links (frontend domain)
# This should be your production frontend URL (not backend URL)
# Example for production:
BASE_URL=https://biometrictesting.fiscoclic.mx
# For local development:
# BASE_URL=http://localhost:3000

# CORS Configuration (comma-separated list of allowed origins)
# Backend will allow requests from these origins
# If not set, defaults to: localhost:3000, localhost:3001, biometrictesting.fiscoclic.mx
# Example for production:
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,https://biometrictesting.fiscoclic.mx,https://www.biometrictesting.fiscoclic.mx


# Meeting Bot Configuration
MAX_RECORDING_DURATION=60
INACTIVITY_LIMIT=10
ACTIVATE_INACTIVITY_AFTER=2
BOT_MAX_LIFETIME_MINUTES=90

# Video Compression Settings (optional - reduces file size)
# Enable automatic post-processing compression (requires more CPU)
ENABLE_VIDEO_COMPRESSION=false
# Recording bitrates (lower = smaller files, but lower quality)
# VIDEO_BITRATE=2500000  # 2.5 Mbps (default, good quality)
# VIDEO_BITRATE=1500000  # 1.5 Mbps (smaller files, acceptable quality)
# VIDEO_BITRATE=1000000  # 1 Mbps (very small files, lower quality)
# AUDIO_BITRATE=128000   # 128 kbps (default)
# AUDIO_BITRATE=96000    # 96 kbps (smaller audio)

# Timezone and Locale Configuration
# Mexico Timezones:
# - America/Mexico_City (Central Time - Most of Mexico, including CDMX, Guadalajara, Monterrey)
# - America/Cancun (Eastern Time - Quintana Roo)
# - America/Chihuahua (Mountain Time - Chihuahua, Sinaloa)
# - America/Tijuana (Pacific Time - Baja California)
# - America/Mazatlan (Mountain Time - Baja California Sur, Nayarit)
TIMEZONE=America/Mexico_City
LOCALE=es-MX

# OpenAI Configuration (optional - for AI-powered summaries)
OPENAI_API_KEY=your_api_key_here
# Recommended models for different transcript lengths:
# - gpt-4o-mini (default, 128k tokens, cost-effective, handles long meetings 1+ hours)
# - gpt-4o (128k tokens, highest quality)
# - gpt-4-turbo (128k tokens, high quality)
# - gpt-3.5-turbo (4k tokens, cheapest, only for short meetings <15 min)
# - gpt-3.5-turbo-16k (16k tokens, for meetings up to ~1 hour)
# For meetings over 1 hour, system automatically uses chunked summarization
OPENAI_MODEL=gpt-4o-mini

# Meeting Keywords Tracking (JSON array)
# Customize keywords to track in meetings
MEETING_KEYWORDS=["acción","tarea","fecha límite","decisión","acuerdo","asignar","responsable","próximos pasos","prioridad","presupuesto","cronograma","hito","riesgo","problema","bloqueador"]

# Database Configuration
DATABASE_PATH=./meeting_bot.db

# JWT Configuration
JWT_SECRET=change_me_in_production_to_a_secure_random_string

# Email Configuration (optional - for meeting summary delivery)
# Email provider: 'gmail', 'sendgrid', or 'smtp'
EMAIL_PROVIDER=gmail
EMAIL_USER=your_email@gmail.com
# For Gmail: Use App Password (https://support.google.com/accounts/answer/185833)
EMAIL_PASSWORD=your_app_password_here

# For SendGrid (alternative to Gmail)
# EMAIL_PROVIDER=sendgrid
# SENDGRID_API_KEY=your_sendgrid_api_key

# For Custom SMTP (alternative)
# EMAIL_PROVIDER=smtp
# SMTP_HOST=smtp.your-provider.com
# SMTP_PORT=587
# SMTP_SECURE=false
# EMAIL_USER=your_email@example.com
# EMAIL_PASSWORD=your_password

# AWS S3 Configuration (optional - for video storage)
# If configured, videos will be uploaded to S3 after recording with automatic retry
# AWS_ACCESS_KEY_ID=your_aws_access_key_id
# AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=your-bucket-name
# AWS_S3_DELETE_LOCAL_AFTER_UPLOAD=false  # Set to 'true' to delete local files after S3 upload

# S3 Bucket Policy (apply this to your bucket for public video access):
# {
#   "Version": "2012-10-17",
#   "Statement": [
#     {
#       "Sid": "PublicReadGetObject",
#       "Effect": "Allow",
#       "Principal": "*",
#       "Action": "s3:GetObject",
#       "Resource": "arn:aws:s3:::your-bucket-name/videos/*"
#     }
#   ]
# }
#
# S3 CORS Configuration (REQUIRED for video playback from web browsers):
# Apply the CORS configuration from s3-cors-config.json to your S3 bucket
# This allows browsers to load videos from S3 across different domains

# Webhook configuration (optional):
# If you want the bot to POST events (like meeting.bot_joined) to an external
# service, set WEBHOOK_URL to the full URL. The bot will POST JSON payloads
# with shape: { event: "meeting.bot_joined", data: { meeting_id, meeting_url, joined_at } }
WEBHOOK_URL=

